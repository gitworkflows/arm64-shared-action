# Arch Test GitHub Action

Run architecture tests using Debian’s `arch-test` tool. Useful for checking compatibility with `armel`, `armhf`, etc.

## Example Usage

```yaml
uses: gitworkflows/arm-shared-action@v1
